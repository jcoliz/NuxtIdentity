# V1 Scope

- JWT token generation from ASP.NET Core Identity
- `/api/auth/login`, `/api/auth/logout`, `/api/auth/refresh` endpoints
- Compatible with nuxt-auth's `authjs` or `local` providers
- User registration/management helpers
